import Login from '@/modules/Connect/Login';
export default function Connect() {
  return <div style={{ height: 'calc(100vh - 60px)' }} className="flex items-center justify-center w-[full]">
    <Login />
  </div>;
}
